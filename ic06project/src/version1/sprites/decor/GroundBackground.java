package version1.sprites.decor;


/**
 * A GroundBackground is a <code>Ground</code> on which players can walk but which are in the background.
 * Elements like missiles can go through these GroundBackground.
 *
 */
public class GroundBackground extends Ground{
	
	public GroundBackground(int _x, int _y, int _w, int _h){
		super(_x,_y,_w,_h);
	}

}
